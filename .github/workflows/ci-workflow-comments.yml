name: Continuous integration Add comments

on:
  workflow_run:
    workflows: ["Continuous integration"]
    types:
      - completed

jobs:
  use_output:
    runs-on: ubuntu-latest
    steps:
      - name: Use output
        run: echo "${{ github.event.workflow_run.outputs.interaction_data }}"
